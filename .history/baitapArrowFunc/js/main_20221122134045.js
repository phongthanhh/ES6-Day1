const colorList = ['pallet', 'viridian', 'pewter', 'cerulean', 'vermil', 'lion', 'lavender', 'celadon', 'saffaron', 'fuschia', 'cinnabar'];
const loadBtn = () => {
    let getBtn = document.querySelector('#colorContainer')
    colorList.forEach(color => {
        console.log(color)
    })

}